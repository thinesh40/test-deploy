name: CI

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on:  ubuntu-latest

    steps:

      - name: Run a multi-line script
        run: |
          echo runnning from libraries
